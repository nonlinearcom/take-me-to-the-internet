// Slugs of the `pages` collection that are standalone pages served at /:slug;
// every other published page is an article served at /articles/:slug.
export const STATIC_PAGE_SLUGS = ['privacy', 'terms', 'about']
